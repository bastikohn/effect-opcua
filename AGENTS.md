# AGENTS.md

- Use `opensrc` for raw source access to this project's `effect` v4 and `node-opcua` libraries.
- If `opensrc` is not available in the host's `$PATH`, try using the project's flake or raw Nix commands to access/run it.
